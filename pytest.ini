[pytest]
markers =
    network: tests that require internet access (API calls, web scraping, etc.)
    slow: tests that take more than 5 seconds
addopts = -m "not network and not slow"
testpaths = .
python_files = test_*.py
