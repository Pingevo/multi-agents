# Opportunity Solution Tree — AI เป็นพนักงานเสมือนมนุษย์

> ไฟล์นี้คือ OST (Opportunity Solution Tree) ที่ไล้จากสิ่งที่จับใจความจาก user
> โครงสร้าง: Outcome → Opportunity → connect the dots → "ต้องมีอะไร"
> "ต้องมี" ที่ยังไม่มี = milestone/issue ในอนาคต
> "มีแล้ว" = ไม่ต้องทำใหม่

---

## Outcome

AI เป็นพนักงานเสมือนมนุษย์ที่ทำงานแทนคนจริง

---

## Opportunity 1: user เคยสั่ง Claude ทุกวัน ต้องมารันเอง → อยากให้อัตโนมัติ

```
ทำอัตโนมัติได้ยังไง?
└─ ตั้งเวลา → ต้องมี: ระบบ scheduler จริง
   │
   ├─ รันตอนไหน? → ต้องมี: ตั้งเวลาได้
   │  └─ timezone ไหน? → ต้องมี: timezone ไทย
   │
   ├─ รันเสร็จแล้วทำอะไร?
   │  ├─ สร้าง content → (มีแล้ว)
   │  ├─ เก็บในคลัง → (เชื่อมไป Opportunity 6)
   │  └─ โพสต์เลย? → (เชื่อมไป Opportunity 5)
   │
   └─ รู้ว่าวันไหนทำอะไร?
      └─ ต้องมี: ระบบ content calendar
         ├─ AI วางแผนรายเดือน → ต้องมี: AI วางแผนได้
         ├─ ดูปฏิทิน → ต้องมี: UI ปฏิทิน
         └─ แก้แผนได้ → ต้องมี: ฟังก์ชันแก้
```

---

## Opportunity 2: user ทำคนเดียว → อยากให้ทำเป็นทีม

```
ทำเป็นทีมได้ยังไง?
├─ แบ่ง role → (มีแล้ว: ระบบทีม + Manager)
├─ 1 ตัวคุม → (มีแล้ว: Manager วางแผน/ตรวจ/แก้/ปล่อย)
└─ แบ่งทีมตามแบรนด์ → ต้องมี: Brand entity (User → Brand → Team → Agent)
   ├─ ทำไมต้องมี Brand? → เพราะ user ดูแลหลายแบรนด์ ทีมกระจัดกระจายถ้าไม่จัดกลุ่ม
   ├─ แต่ละทีมรู้แบรนด์ตัวเองไหม? → (เชื่อมไป Opportunity 8)
   └─ 1 แบรนด์ = หลายทีมได้ (เช่น ทีม MKT, ทีม Admin ในแบรนด์เดียว)
```

---

## Opportunity 3: user อยากให้คุณภาพสม่ำเสมอ ไม่ผ่านไม่ปล่อย

```
คุมคุณภาพยังไง?
├─ ตรวจก่อนปล่อย → (มีแล้ว: review loop)
├─ ไม่ผ่านให้แก้ → (มีแล้ว: Manager สั่งแก้)
│
└─ ทำซ้ำๆ แล้วดีขึ้นไหม?
   └─ ต้องมี: ระบบเรียนรู้
      ├─ จำครั้งก่อน → ต้องมี: ระบบ memory
      │  └─ เก็บอะไร? → ประสบการณ์/feedback/ผิดถูก
      ├─ รู้ว่า user ปฏิเสธอะไร → ต้องมี: เก็บ feedback
      └─ ปรับตัว → ต้องมี: tune agent
         └─ ปรับอะไร? → persona/criteria/โทน
```

---

## Opportunity 4: user ทำซ้ำๆ → ต้องจำได้ข้ามวัน

```
จำอะไร?
├─ ประสบการณ์ทำงาน → (เชื่อมไป Opportunity 3)
├─ คุยกับ user ครั้งก่อน → ต้องมี: ระบบ memory
│  ├─ เก็บที่ไหน? → ต้องมี: storage
│  └─ ดึงกลับมายังไง? → ต้องมี: ดึงเข้า context ตอนทำงาน
└─ งานที่เคยทำ → ต้องมี: ประวัติงาน
   └─ (มีบางส่วน: HistoryStore)
```

---

## Opportunity 5: user อยากให้โพสต์ social จริง

```
จะโพสต์อะไร?
└─ content (รูป + ข้อความ)
   │
   ├─ ข้อความมาจากไหน?
   │  ├─ สร้างเอง → (มีแล้ว)
   │  └─ ดึงจากคลัง → (เชื่อมไป Opportunity 6)
   │
   ├─ รูปมาจากไหน?
   │  ├─ สร้างเอง → (มีแล้ว)
   │  └─ ดึงจากสินค้า → (เชื่อมไป Opportunity 7)
   │
   ├─ โพสต์ยังไง?
   │  └─ เชื่อม platform API → ต้องมี: ระบบโพสต์ social
   │     ├─ FB/IG/TikTok คนละ API → ต้องมี: adapter แต่ละ platform
   │     ├─ token เก็บยังไง? → ต้องมี: ระบบเข้ารหัส credential
   │     └─ โพสต์ผิดลบได้ไหม? → ต้องมี: rollback
   │
   ├─ ก่อนโพสต์ user คุมได้ไหม?
   │  └─ ต้องมี: ระบบอนุมัติ
   │     ├─ ดู preview ก่อน → ต้องมี: preview
   │     ├─ แก้ก่อนโพสต์ → ต้องมี: ฟังก์ชันแก้
   │     └─ กดอนุมัติ → ต้องมี: ปุ่มอนุมัติ
   │
   └─ หลังโพสต์รู้ผลไหม?
      └─ ต้องมี: ระบบวัดผล
         └─ ดึง engagement จาก platform → ต้องมี: ดึง like/share/view
```

---

## Opportunity 6: user อยากให้มีคลัง content (เก็บ/ดูย้อนหลัง/แก้/ส่งออก)

```
เก็บอะไร?
└─ content + สินค้าไหน + format ไหน + platform ไหน + วันที่ + สถานะ
   │
   ├─ เก็บได้ → ต้องมี: storage
   ├─ ดูย้อนหลังได้ → ต้องมี: UI รายการ + ค้นหา
   ├─ แก้ได้ → ต้องมี: ฟังก์ชันแก้ + เก็บเวอร์ชันเก่า
   └─ ส่งออกได้ → ต้องมี: export/download
```

---

## Opportunity 7: user อยากให้ AI รู้สินค้า ราคา โปร รูป กลุ่มเป้าหมาย

> **Pivot 2026-08-06 (ADR-0006):** assumption เดิม "สินค้าอยู่ใน MongoDB" ผิด — MongoDB `itsAgents` ว่างเปล่า + สิทธิ์ readWrite = เก็บของเรา ไม่ใช่ดึงสินค้า

```
# Assumption (เดิม — ผิด): สินค้าอยู่ใน MongoDB แล้ว, เรามีสิทธิ์ read
# Assumption (ใหม่): MongoDB = data storage ของระบบเรา, สินค้าอยู่ที่อื่น (ยังไม่รู้ที่)

ข้อมูลอยู่ที่ไหน?
├─ สินค้า/ราคา/โปร/รูป → ยังไม่รู้ที่ (พัก — ดู ADR-0006)
│  ├─ อาจเป็น: database อื่น, REST API, หรือ user กรอก/อัปโหลดเอง
│  ├─ AI ใช้ยังไง? → ต้องมี: ถาม/ค้นหา/ดึงรายละเอียด
│  └─ DB ล่ม? → ต้องมี: บอก user ไม่ใช่แฮงค์
│
├─ MongoDB (itsAgents) → เก็บข้อมูลระบบเรา (ไม่ใช่ดึงสินค้า)
│  ├─ ย้าย JSON → MongoDB ทีละ store (M6.5: Data Persistence Migration)
│  └─ ตาม ADR-0003 (JSON = ชั่วคราว, store classes = seam สำหรับย้าย)
│
└─ กลุ่มเป้าหมาย → ไม่ใช่ MongoDB → ต้องมี: ระบบจัดการความรู้แบรนด์
   ├─ user กรอก → ต้องมี: UI
   ├─ เก็บ per Brand → ต้องมี: Brand entity (เปลี่ยนจาก per team เป็น per Brand)
   └─ AI ใช้ตอนทำงาน → ต้องมี: ดึงเข้า persona ผ่าน Brand ของทีม
```

---

## Opportunity 8: user อยากให้แต่ละแบรนด์รู้ตัวเอง (ย้ายจากทีม → แบรนด์)

```
แบรนด์รู้อะไรของตัวเอง?
├─ กลุ่มเป้าหมาย → (เชื่อมไป Opportunity 7)
├─ โทน/สไตล์ → ต้องมี: เก็บใน Brand entity (brand_context)
├─ คำต้องห้าม → ต้องมี: เก็บใน Brand entity (brand_context)
└─ AI ใช้ตอนทำงาน → ต้องมี: ดึงเข้า persona ผ่าน Brand ของทีม
   ├─ ทีมในแบรนด์เดียวกัน → ใช้ brand_context เดียวกัน (ไม่ drift)
   └─ แบรนด์ A ไม่ปน แบรนด์ B → ต้องมี: แยกข้อมูลตาม Brand
```

---

## Opportunity 9: user อยากให้สร้าง product desc/spec/SEO/caption

```
สร้างจากอะไร?
└─ สินค้า → (เชื่อมไป Opportunity 7)
   │
   ├─ กี่รูปแบบ? → desc/spec/SEO/caption (4 อย่าง)
   │  └─ แต่ละรูปแบบต่างกันยังไง? → ต้องคิด: structure แต่ละ format
   │
   ├─ ปรับตาม platform ไหม? → ต้องมี: ปรับ format FB/IG/TikTok
   │  └─ ต่างกันยังไง? → ต้องคิด: แต่ละ platform ต้องการอะไร
   │
   ├─ ใช้ความรู้แบรนด์ไหม? → (เชื่อมไป Opportunity 7, 8)
   │
   └─ ภาษาอะไร? → ไทย + อังกฤษ
      └─ ต้องมี: รองรับทั้งสองภาษา
```

---

## Opportunity 10: user อยากให้วิเคราะห์วิดีโอ TikTok (ฮุค/ตัดต่อ/เอนเกจ)

```
รับอะไร?
└─ ลิงก์ TikTok → ต้องมี: ระบบวิเคราะห์วิดีโอ
   │
   ├─ ดึงอะไร? → วิดีโอ + metadata (caption/sound/hashtag/views/likes)
   │  └─ ดึงยังไง? → ต้องคิด: ดึงไฟล์ หรือ ส่ง URL ให้ AI
   │
   ├─ วิเคราะห์อะไร? → ฮุค 3 วินาทีแรก / จังหวะตัดต่อ / เอนเกจ / CTA
   │  └─ ใครวิเคราะห์? → AI (vision model)
   │
   └─ ส่งอะไรกลับ? → รายงานที่อ่านแล้วเข้าใจ
```

---

## Opportunity 11: user อยากให้ติดตามคู่แข่ง

```
รู้ว่าคู่แข่งคือใคร?
└─ ต้องมี: user บอก หรือ AI หาเอง
   │
   ├─ ดึงอะไร? → content คู่แข่ง (โพสต์/รูป/วิดีโอ/ราคา)
   │  └─ ดึงยังไง? → ต้องมี: เชื่อม platform หรือ scrape
   │
   ├─ เปรียบเทียบกับอะไร? → สินค้าตัวเอง (เชื่อมไป Opportunity 7)
   │
   └─ ส่งอะไรกลับ? → สรุป + ข้อแตกต่าง + โอกาส
```

---

## Opportunity 12: user อยากให้เชื่อมโยง สินค้า→คู่แข่ง→content

```
เชื่อมยังไง?
├─ สินค้า → (มี: Opportunity 7)
├─ เทียบกับคู่แข่ง → (มี: Opportunity 11)
└─ ทำ content ตอบโต้ → ต้องมี: AI รู้ทั้ง 2 อย่าง แล้วสร้างได้
   └─ ใช้ความสามารถที่มี → (มี: สร้าง content + ความรู้แบรนด์)
```

---

## Opportunity 13: user อยากให้วางแผน Monthly Content โดยโพสวันละอย่างน้อย 1 โพส สัดส่วน Short Video และ Artwork 50:50 ต่อเดือน

```
วางแผนรายเดือนได้ยังไง?
└─ ต้องมี: ระบบวางแผน content รายเดือน
   │
   ├─ กี่โพสต์ต่อเดือน? → อย่างน้อย 1 โพสต์/วัน
   │  └─ ใครกำหนด? → user กำหนด หรือ AI คำนวณ
   │
   ├─ สัดส่วนประเภท? → Short Video : Artwork = 50:50
   │  ├─ ใครกำหนดสัดส่วน? → user กำหนด
   │  ├─ ปรับสัดส่วนได้ไหม? → ต้องมี: ตั้งสัดส่วนได้
   │  └─ มีประเภทอื่นได้ไหม? → ต้องคิด: ให้ user เพิ่มประเภทได้
   │
   ├─ กระจายตามวันยังไง?
   │  ├─ ต้องมี: AI จัดวันให้
   │  ├─ ดูในปฏิทิน → (เชื่อมไป Opportunity 1: content calendar)
   │  └─ แก้ได้ → ต้องมี: ฟังก์ชันแก้
   │
   ├─ แต่ละโพสต์ทำอะไร?
   │  ├─ Short Video → (เชื่อมไป: ระบบสร้างวิดีโอ — มีแล้ว)
   │  └─ Artwork → (เชื่อมไป: ระบบสร้างรูป — มีแล้ว)
   │
   └─ ใช้ข้อมูลอะไรวางแผน?
      ├─ สินค้า → (เชื่อมไป Opportunity 7)
      ├─ ความรู้แบรนด์ → (เชื่อมไป Opportunity 7, 8)
      └─ คู่แข่ง → (เชื่อมไป Opportunity 11)
```

---

## Opportunity 14: user อยากกำหนดรูปแบบการตอบของ agent ได้ (เช่น วิเคราะห์สินค้าตาม format ตายตัว)

ตัวอย่าง format ที่ user ให้มา (วิเคราะห์สินค้า):

1. **จุดแข็งหลักและนวัตกรรม (Core Strengths & Innovation / USP)**
   - คำถามนำทาง: อะไรคือสิ่งที่สินค้านี้ทำได้ แต่คู่แข่งในตลาดที่ราคาถูกกว่าทำไม่ได้?
2. **กลุ่มเป้าหมายและกำลังซื้อ (Targeting & Purchasing Power)**
   - คำถามนำทาง: ใครคือกลุ่มคนที่ยอมจ่ายแพงกว่าเพื่อสเปกที่ดีที่สุด?
3. **การเพิ่มมูลค่าเฉลี่ยต่อออเดอร์ (Upselling / Value Bundle Strategy)**
   - คำถามนำทาง: ผู้ใช้สินค้าประเภทนี้จะต้องการอะไรเพิ่มเติมอีกไหม?
4. **การบริหารความเสี่ยงและแนวทางแก้ไข (Risk Management & Operations)**
   - คำถามนำทาง: ปัญหาการใช้งานแบบใดที่ลูกค้ามักโทษสินค้า?

```
กำหนดรูปแบบการตอบได้ยังไง?
└─ ต้องมี: ระบบกำหนด output format ของ agent
   │
   ├─ เก็บ format ที่ไหน? → ต้องมี: storage สำหรับ format template
   │  └─ เก็บ per team / per task type? → ต้องคิด
   │
   ├─ ใครกำหนด? → user กำหนด
   │  ├─ กรอกเอง → ต้องมี: UI สร้าง/แก้ format
   │  └─ ใช้ format สำเร็จรูป? → ต้องมี: คลัง format
   │
   ├─ ใช้ตอนไหน? → ตอน agent ตอบ/ทำงาน
   │  └─ ต้องมี: ดึง format เข้า prompt ตอนทำงาน
   │
   ├─ กี่แบบ?
   │  ├─ วิเคราะห์สินค้า → format 4 หัวข้อ (ตัวอย่างข้างบน)
   │  ├─ วิเคราะห์คู่แข่ง → ต้องคิด: format อะไร
   │  ├─ วิเคราะห์วิดีโอ → (เชื่อมไป Opportunity 10)
   │  └─ อื่นๆ → ต้องมี: user สร้าง format เองได้
   │
   └─ เปลี่ยน format ได้ไหม?
      ├─ แก้ได้ → ต้องมี: ฟังก์ชันแก้
      └─ ใช้หลาย format พร้อมกัน → ต้องคิด
```

---

## สรุป "ต้องมี" ทั้งหมด (ที่ยังไม่มี)

| ต้องมี | มาจาก Opportunity |
|---|---|
| ระบบ scheduler จริง + timezone ไทย | 1 |
| ระบบ content calendar | 1 |
| ระบบเรียนรู้ (memory + feedback + tune) | 3, 4 |
| ระบบโพสต์ social + adapter แต่ละ platform | 5 |
| ระบบเข้ารหัส credential | 5 |
| rollback | 5 |
| ระบบอนุมัติ (preview + แก้ + ปุ่ม) | 5 |
| ระบบวัดผล (ดึง engagement) | 5 |
| ระบบคลัง content (เก็บ/ดู/แก้/export) | 6 |
| ระบบเชื่อม MongoDB | 7 |
| Brand entity (User → Brand → Team → Agent) | 2, 8 |
| ระบบจัดการความรู้แบรนด์ (brand_context ที่ Brand ไม่ใช่ Team) | 7, 8 |
| ระบบสร้าง product content (4 format + ปรับ platform + 2 ภาษา) | 9 |
| ระบบวิเคราะห์วิดีโอ TikTok | 10 |
| ระบบติดตามคู่แข่ง | 11 |
| ระบบวางแผน content รายเดือน (สัดส่วน + จัดวัน) | 13 |
| ระบบกำหนด output format ของ agent (เก็บ/ใช้/แก้ format) | 14 |

---

## สังเกต

- บาง Opportunity เชื่อมกัน (เช่น 7→9→5 คือ สินค้า→content→โพสต์)
- บางกิ่งมี "ต้องคิด" = ยังไม่รู้ ต้องดูของจริง (เช่น MongoDB field, แต่ละ platform ต่างกันยังไง)
- บาง Opportunity มีแล้วหมด (เช่น 2 = ทีม + Manager) — แต่ "แบ่งทีมตามแบรนด์" ยังไม่มี ต้องสร้าง Brand entity
- Opportunity 12 ไม่มี "ต้องมี" ใหม่ เพราะเชื่อมของที่มี
- Opportunity 13, 14 เชื่อมกับหลาย Opportunity (ใช้ของที่มี + ต้องสร้างใหม่)
