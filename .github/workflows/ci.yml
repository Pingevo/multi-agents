name: CI

on:
  push:
    branches: ['**']
  pull_request:
    branches: [dev, main]

jobs:
  backend-tests:
    name: Backend Tests (Pytest)
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run pytest (excluding network tests)
        env:
          OPENROUTER_API_KEY: test-key-fake
          LLM_PROVIDER: openrouter
          LLM_API_KEY: test-key-fake
          LLM_BASE_URL: https://openrouter.ai/api/v1
          LOCAL_LLM_FALLBACK: "false"
        run: |
          python -m pytest -v --tb=short -m "not network and not slow"

  frontend-lint:
    name: Frontend Lint & Build
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm install

      - name: Lint (oxlint)
        working-directory: frontend
        run: npm run lint

      - name: Type check (tsc)
        working-directory: frontend
        run: npx tsc --noEmit

      - name: Build (vite)
        working-directory: frontend
        run: npm run build

  frontend-e2e:
    name: Full-Stack E2E (Playwright + Backend + Fake LLM)
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install backend dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Install root dependencies
        run: npm install

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm install

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps

      - name: Run Playwright E2E tests (full-stack)
        run: npx playwright test
        env:
          CI: "true"
          LLM_PROVIDER: openrouter
          LLM_API_KEY: test-key-fake
          LLM_BASE_URL: http://127.0.0.1:11435
          LOCAL_LLM_FALLBACK: "false"
          USE_DEV_LOGIN: "true"
          AGENT_APP_DATA_DIR: /tmp/agent-e2e-data
